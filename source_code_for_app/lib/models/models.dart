class Models {
  DateTime pickedDate;
  String weather;
  int totalDays;
  String firmness;
  Models(
      {required this.firmness,
      required this.pickedDate,
      required this.totalDays,
      required this.weather});
}
